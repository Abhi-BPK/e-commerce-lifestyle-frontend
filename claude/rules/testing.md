# Testing Rules
- Test files live next to the component: Button.test.jsx
- Use React Testing Library, not Enzyme
- Test behavior, not implementation details
- Every new component needs at least one test